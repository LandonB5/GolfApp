type Note @model @auth(rules: [{allow: owner}]) {
  id: ID!
  name: String!
  description: String
  image: String
} 
type UserProfile @model @auth(rules: [{ allow: owner }]) {
  id: ID!
  name: String!
  email: String!
  age: Int
  bio: String
  photoURL: String
}